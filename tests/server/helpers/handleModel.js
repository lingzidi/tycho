import handleModel from '../../../server/helpers/handleModel';

describe('the handleModel helper function', () => {
  it('should return a 400 if an error is specified', () => {
    // ...
  });

  it('should return a 404 if no model is specified', () => {
    // ...
  });

  it('should return the model if exists and no error is specified', () => {
    // ...
  });
});