import controller from '../../../../server/auth/github/github.controller';

describe('the github controller', () => {
	describe('the onAuth functiion', () => {
		it('should assign the user object to the request', () => {
			
		});
	});
});