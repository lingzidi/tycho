import ensureAuthenticated from '../../../server/helpers/auth';

describe('the ensureAuthenticated helper function', () => {
  it('should call the next() callback if the user is defined', () => {
    // ...
  });

  it('should return a 403 status if req.user is not defined', () => {
    // ...
  });
});